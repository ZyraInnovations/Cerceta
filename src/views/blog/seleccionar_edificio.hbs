<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seleccionar Edificio - Blog</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Main content styling */
      
        /* Form container */
        .form-container {
            background: #ffffff;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
            max-width: 400px;
            width: 100%;
            text-align: center;
            border-top: 8px solid #C92458;
            transition: transform 0.3s ease;
        }

        .form-container:hover {
            transform: translateY(-10px);
        }

        /* Heading styling */
        .form-container h2 {
            color: #1E99D3;
            font-size: 1.8em;
            margin-bottom: 20px;
            font-weight: 600;
        }

        /* Label and Select styling */
        .form-container label {
            display: block;
            font-size: 1em;
            margin-bottom: 10px;
            color: #666567;
        }

        .form-container select {
            width: 100%;
            padding: 12px;
            margin-bottom: 20px;
            font-size: 1em;
            border: 2px solid #1E99D3;
            border-radius: 8px;
            color: #413d4b;
            background-color: white;
            font-weight: bold;
        }

        /* Button styling */
        .form-container button {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 12px;
            background-color: #7CB646;
            color: #ffffff;
            font-size: 1.1em;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.3s ease;
            width: 100%;
            font-weight: bold;
        }

        .form-container button:hover {
            background-color: #C92458;
            transform: scale(1.05);
        }

        /* Icon styling */
        .form-container button i {
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2><i class="fas fa-building"></i> Seleccionar Edificio</h2>
        <form action="/ver_blog_admin" method="get">
            <label for="edificio">Edificio:</label>
            <select name="edificio_id" id="edificio" required>
                <option value="" disabled selected>Seleccione un edificio</option>
                {{#each edificios}}
                    <option value="{{this.id}}">{{this.nombre}}</option>
                {{/each}}
            </select>
            
            <button type="submit"><i class="fas fa-eye"></i> Ver Blog</button>
        </form>
    </div>
</body>
</html>
