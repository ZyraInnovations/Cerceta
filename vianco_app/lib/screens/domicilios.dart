import 'package:flutter/material.dart';

class DomiciliosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Domicilios")),
      body: Center(child: Text("Bienvenido a Domicilios")),
    );
  }
}
